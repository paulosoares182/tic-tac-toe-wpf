<UserControl x:Class="TicTacToe.Views.SettingsView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TicTacToe.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Margin="0, 0, 0, 20">
                <TextBlock>Jogador 1</TextBlock>
                <TextBox Text="{Binding Path=Player1Nickname, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Nickname"/>
            </StackPanel>

            <StackPanel Margin="0, 0, 0, 20">
                <TextBlock>Jogador 2</TextBlock>
                <TextBox Text="{Binding Path=Player2Nickname, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Nickname"/>
            </StackPanel>

            <Button Width="200" Command="{Binding StartCommand}" Content="Começar"/>
        </StackPanel>
    </Grid>
</UserControl>
